apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mcp-atlassian-tls
  namespace: kagent
spec:
  secretName: mcp-atlassian-tls
  dnsNames:
    - mcp-atlassian.local
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
