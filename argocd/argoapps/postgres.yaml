apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres
  namespace: argocd
spec:
  project: default
  source:
      repoURL: https://github.com/HiddenTechVillage/k8s-dev.git
      targetRevision: HEAD
      path: cluster-services/postgres
  destination:
      server: https://kubernetes.default.svc
      namespace: postgres
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
