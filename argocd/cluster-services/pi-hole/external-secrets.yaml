apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: pi-hole
  namespace: pihole
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  data:
  - secretKey: PI_HOLE_ADMIN_PASSWORD
    remoteRef:
      key: "6fb1147b-59fb-4463-ae16-b31f0180d1eb"