apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-application
  namespace: argocd
spec:
  project: default
  sources:
      - repoURL: https://github.com/HiddenTechVillage/k8s-dev.git
        targetRevision: HEAD
        path: argocd/applications/applications/test-application
        directory:
          recurse: true
  destination:
      server: https://kubernetes.default.svc
      namespace: default
  syncPolicy:
      syncOptions:
      - CreateNamespace=true
      automated:
        selfHeal: true
        prune: true
