apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kagent-local-tls-certs
  namespace: kagent-local
spec:
  secretName: kagent-local-tls-certs
  commonName: kagent.kagent.svc.cluster.dev
  dnsNames:
    - kagent-local.kagent.svc.cluster.dev
    - kagent-local.dev
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
